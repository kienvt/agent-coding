---
name: init-plan
description: Phase 1 — Analyze requirements and initialize project planning
argument-hint: requirementFile=<path> repoName=<name> projectId=<id> codeRepos=<repos>
disable-model-invocation: true
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

Execute the **init-plan** skill workflow. Context:

$ARGUMENTS

Follow all steps in `.claude/skills/init-plan/SKILL.md` precisely.
