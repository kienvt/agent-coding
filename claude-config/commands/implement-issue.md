---
name: implement-issue
description: Phase 2 — Implement a single GitLab issue in the target code repository
argument-hint: issueIid=<iid> issueProjectId=<id> repoName=<name> projectSlug=<slug>
allowed-tools: Skill, Read, Write, Edit, Bash, Glob, Grep
---

Invoke the `implement-issue` skill using the Skill tool with the following context:

$ARGUMENTS
